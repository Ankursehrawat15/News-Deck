<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.news_deck">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_news_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_news_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewsDeck">
        <activity android:name=".splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity">

        </activity>
    <activity android:name=".Detail.NewsDetail">

    </activity>
        <activity android:name=".Welcome_Screen">

        </activity>
    </application>

</manifest>